<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/teams/edit-team/{{team.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{team.name}}" name="name" class="form-control" placeholder="Nombre del equipo">                        </div>
                        <div class="form-group"
                            <button class="btn btn-success btn-block">
                                Save
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

